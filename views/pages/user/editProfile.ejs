<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/styles/auth.css">
</head>

<body>
    <%- include("../../layout/flash") %>
    <%- include("../../layout/header") %>

    <div class="container">
        <div class="sidebar">
            <%- include("../../layout/sidebar") %>
        </div>
        <form action="/profile/edit-profile" method="post" enctype="multipart/form-data">
            <div class="header">
                <h2>Edit Profile</h2>
            </div>
            <div class="upload">
                <label for="upload">Upload</label>
                <input type="file" name="profilePic" id="upload" hidden>
                <div class="user-image">
                    <img src="/uploads/<%= profile ? profile.profilePic : " default.png" %>" alt="image">
                </div>
            </div>
            <div class="form-group">
                <label for="name">Name : </label>
                <input type="text" name="name" value="<%= profile ? profile.name : user.name %>" required>
                <div class="error-fedback">
                    <p>
                        <%= error.name %>
                    </p>
                </div>
            </div>
            <div class="form-group">
                <label for="email">Email : </label>
                <input type="email" name="email" value="<%= user.email %>" disabled>
            </div>
            <div class="form-group">
                <label for="title">Title : </label>
                <input type="text" name="title" value="<%= profile ? profile.title : "" %>" required>
                <div class="error-fedback">
                    <p>
                        <%= error.title %>
                    </p>
                </div>
            </div>
            <div class="form-group">
                <label for="bio">Bio : </label>
                <input type="text" name="bio" value="<%= profile ? profile.bio : "" %>" required>
                <div class="error-fedback">
                    <p>
                        <%= error.bio %>
                    </p>
                </div>
            </div>
            <div class="form-group">
                <label for="website">Website : </label>
                <input type="text" name="website" value="<%= profile ? profile.website : "" %>" >
                <label for="facebook">Facebook : </label>
                <input type="text" name="facebook" value="<%= profile ? profile.facebook : "" %>" >
                <label for="github">Github : </label>
                <input type="text" name="github" value="<%= profile ? profile.github : "" %>" >
                <label for="twitter">Twitter : </label>
                <input type="text" name="twitter" value="<%= profile ? profile.twitter : "" %>" >
            </div>
            <button type="submit">Save</button>
        </form>
    </div>


    <%- include("../../layout/footer") %>
<script src="/scripts/profile.js"></script>
</body>
</html>